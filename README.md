# CrashNet — prototype
Overview: CrashNet is an IoT + ML system for realtime accident detection and emergency alerts.

Repos:
- crashnet-ml: ML & inference
- crashnet-api: REST endpoints
- crashnet-ws: WebSocket realtime server

Run locally:
1. Clone all repos as sibling folders
2. cd CrashNet
3. docker-compose up --build
